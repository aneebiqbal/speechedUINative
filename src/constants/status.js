const DEFAULT = 'DEFAULT';
const LOADING = 'LOADING';
const SUCCESS = 'SUCCESS';
const FAILURE = 'FAILURE';

const Status = Object.freeze({DEFAULT, LOADING, SUCCESS, FAILURE});

export default Status;
